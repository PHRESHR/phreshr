import styled from 'styled-components';
import { media } from 'ui/styles/utils';

export const HomeContainer = styled.div`
  margin-top: -5rem;
`;

export const CollapsedContainer = styled.section`
  position: absolute;
  bottom: 5rem;
  z-index: 1;
`;
